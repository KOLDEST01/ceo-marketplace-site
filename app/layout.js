import './globals.css';

export const metadata = {
  title: 'C.E.O. — Create Every Opportunity',
  description: 'An independent creator marketplace by N-Dependant Productions.'
};

export default function RootLayout({ children }) {
  return <html lang="en"><body>{children}</body></html>;
}
